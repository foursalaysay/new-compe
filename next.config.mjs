/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['example.com', 'source.unsplash.com'], // Add your domain here
    },
  };
  
  export default nextConfig;
  